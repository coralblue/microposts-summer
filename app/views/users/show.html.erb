<!--show.html.erb-->
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %></br>  
     </h1>
<%= link_to 'Followings一覧', followings_user_path(@user) %></br>
<%= link_to 'Followers一覧', followers_user_path(@user) %>
</br></br></br>

私のプロフィールは? </br><span><%= @user.profile %></span></br> 
私の年齢は?</br><%= @user.age %>
    </section>
  </aside>

  <div class="col-md-8">
    <%= render 'follow_form' if logged_in? %>
    <% if @user.microposts.any? %>
      <h3>Microposts (<%= @user.microposts.count %>)</h3>
      <ol class="microposts">
        <%= render @microposts %>
      </ol>
    <% end %>
  </div>
</div>