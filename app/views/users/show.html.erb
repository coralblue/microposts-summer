<!--show.html.erb-->
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %></br>  
     </h1>
<%= link_to 'Followings一覧', followings_user_path(@user) %></br>
<%= link_to 'Followers一覧', followers_user_path(@user) %>
</br></br></br>


      <% if logged_in? %>    
   <!--ログインしていなかったら見ることができない部分を書く     -->
       <% if current_user.age.present? %>
         年齢: </br> 
        <%#= @current_user.age %><!--どちらでも使える模様-->
        <%= @user.age %>
        <% else %>
      <% end %>
      </br> 
        
        <% if current_user.profile.present?  %>
         </br>  
        <span>プロフィール:</br>  <%= @user.profile %></span>
        <% else %>
        <% end %>
        
      <% else %>
      <% end %>

    </section>
  </aside>

  <div class="col-md-8">
    <%= render 'follow_form' if logged_in? %>
    <% if @user.microposts.any? %>
      <h3>Microposts (<%= @user.microposts.count %>)</h3>
      <ol class="microposts">
        <%= render @microposts %>
      </ol>
    <% end %>
  </div>
         <%# if @user.age.blank? %><%#= current_user.profile %><%# if @user.profile.blank? %>